// This public value contains the Render backend URL, never an API key.
// Leave it empty for local use, then replace it with your public Render service URL.
window.APP_CONFIG = {
  API_BASE_URL: "https://jd-ai-analyzer-1.onrender.com"
};
